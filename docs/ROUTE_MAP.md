# Route Map Quick Reference

## Public

`/`, `/login`, `/news`, `/news/[id]`, `/volunteer`, `/shop`, `/shop/[id]`, `/shop/checkout`, `/fundraising`, `/invite/accept`, `/auth/change-password`.

## Member

`/member`, `/member/feed`, `/member/impact`, `/member/profile`, `/member/notifications`, `/member/streak`, `/member/recap`, `/member/recap/[id]`, `/member/activities`, `/member/activities/[id]`, `/member/assignments`.

## Admin

`/admin`, `/admin/dashboard`, `/admin/users`, `/admin/accounts`, `/admin/volunteer-applications`, `/admin/activities`, `/admin/activities/[id]`, `/admin/activities/[id]/matcher`, `/admin/orders`, `/admin/products`, `/admin/articles`, `/admin/articles/new`, `/admin/articles/[id]`, `/admin/gallery`, `/admin/gallery/[id]`, `/admin/campaigns`, `/admin/fundraising`, `/admin/reports`.

## Logistic

`/logistic`, `/logistic/orders`.

Full mapping: `../docs/06-frontend/ROUTING.md` and `../docs/15-ux/UI_PROTOTYPES.md`.
